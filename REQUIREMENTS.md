# 기능 명세서

## 0. Todo Model

| field | type |
| --- | --- |
| id | int |
| text | str |
| completed | bool |
| date | str |
| created_at | datetime |
| update_at | datetime |

## 1. Todo 생성

- 할 일 관리
- 사용자가 입력창에 할 일을 입력하고 “추가” 버튼을 누르거나 enter를 입력하면 Todo가 생성된다. 달력에서 날짜를 선택하고 Todo를 생성하면 해당 Todo에 날짜가 저장된다.
- 입력 항목 : 할 일 + 추가 버튼
- 예외 처리 : 빈 값 입력 시 “할 일을 입력해 주세요.”

## 2. Todo 수정

- 할 일 관리
- 사용자가 생성된 Todo의 “수정” 버튼을 누르면 수정 화면으로 이동한다.
- 입력 항목 : 할 일 + 추가 버튼
- 예외 처리 : 빈 값 입력 시 “할 일을 입력해 주세요.”

### 2-1. Todo 수정 화면

- 할 일 관리
- 사용자가 입력창에 수정할 할 일을 입력하고 “저장” 버튼을 누르거나 enter를 입력하면 Todo가 수정되고, 다시 메인 화면으로 이동한다.
- 입력 항목 : 할 일 + 저장 버튼
- 예외 처리 : 빈 값 입력 시 “할 일을 입력해 주세요.”

## 3. Todo 삭제

- 할 일 관리
- 사용자가 생성된 Todo의 “삭제” 버튼을 누르면 Todo가 삭제된다.
- 입력 항목 : 삭제 버튼
- 입력 조건 : 클릭 이벤트 정상 동작
- 예외 처리 : 버튼 비활성화 또는 오류 발생 시 “잠시만 기다려주세요.”

## 4. Todo 정렬

- 할 일 관리
- 사용자가 생성한 Todo 정렬 (최근 생성이 제일 위로)

## 5. 진행 상태 탭

- 할 일 관리
- 사용자가 클릭한 “전체”, “진행 중”, “완료” 탭에 맞는 Todo만을 조회한다.
- 입력 항목 : 상태(“전체”, “진행 중”, “완료”) 버튼

## 6. Todo 검색

- 할 일 관리
- 사용자가 검색 입력창에 검색할 키워드를 입력하고 “검색” 버튼을 누르면 해당 키워드가 포함된 Todo가 조회된다.
- 입력 항목 : 검색 내용 + 검색 버튼
- 예외 처리 : 빈 값 입력 시 “검색할 내용을 입력해 주세요.”

## 7. Todo 완료

- 할 일 관리
- 사용자가 완료한 Todo의 “완료” 버튼을 누른다. 완료된 Todo는 텍스트에 취소선이 생긴다. 이미 완료된 Todo의 “완료” 버튼을 다시 누르면 “완료” 상태에서 해제되고 취소선이 사라진다.
- 입력 항목 : 삭제 버튼

## 8. 주간 달력

- 주간 달력
- 할 일 입력창 상단에 월요일부터 일요일까지의 주간 달력을 표시한다. 날짜 하단에는 등록된 Todo의 개수를 표시한다. 이전 버튼과 다음 버튼이 있어 사용자가 누른 횟수만큼 이전 주, 다음 주로 이동하여 표시된다. 사용자가 클릭한 날짜에는 해당 날짜로 추가된 Todo가 조회된다. 클릭한 날짜와 오늘 날짜가 같은 경우에는 날짜 상단에 “오늘” 이라고 표시된다.
